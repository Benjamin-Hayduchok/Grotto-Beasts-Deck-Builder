import React from 'react';
import classNames from "classnames";

export default function ContinueMessage() {
    return (
        <p
        className={classNames(
            "flex m-auto"
        )}
    >
        Continue with Google
    </p>
    )
}